OPENPROJECT_URL=http://localhost:8080

# My account -> Access tokens -> API -> Generate
OPENPROJECT_API_KEY=

# Only needed for admin work the REST API cannot do (see tools/op-cli/NOTES.md).
# Prefer, in order: op-cli  ->  REST API  ->  rails runner  ->  browser login.
OPENPROJECT_USERNAME=
OPENPROJECT_PASSWORD=
